import { test, expect, request } from "@playwright/test";

test.describe("ทดสอบอะไรบางอย่าง", () => {
	let TOKEN: string;
	let ORDER_NUMBER: string;

	test('ทดสอบอะไรอย่างที่ 1', async () => {

	});
});